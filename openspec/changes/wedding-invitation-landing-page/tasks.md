## 1. Setup & Core HTML Structure

- [x] 1.1 Add HTML markup for the two distinct photo frames below the couple's names.
- [x] 1.2 Add an `<audio>` element and a custom play/pause button below the photo frames.
- [x] 1.3 Add HTML structure for the virtual letter (to be initially hidden) and the transition arrow button.

## 2. CSS Styling & Responsiveness

- [x] 2.1 Implement Flexbox/Grid layouts to ensure photo frames display side-by-side on desktop and stack vertically on mobile.
- [x] 2.2 Style the music player button to be a medium-sized aesthetic element.
- [x] 2.3 Add CSS classes for the view transition (e.g., classes to hide the main view and classes to fade in the letter view).

## 3. JavaScript Interactivity

- [x] 3.1 Implement click listener on the arrow button to toggle visibility classes between the main view and the letter view.
- [x] 3.2 Implement logic to call `.play()` and `.pause()` on the audio element when the music button is clicked.
- [x] 3.3 Update the music button visually (e.g., swapping icons or text) to reflect the playing/paused state.

## 4. Verification

- [x] 4.1 Test responsiveness across multiple viewport sizes (mobile, tablet, desktop).
- [x] 4.2 Verify audio playback correctly updates the UI and adheres to browser autoplay restrictions (since it requires a click).
