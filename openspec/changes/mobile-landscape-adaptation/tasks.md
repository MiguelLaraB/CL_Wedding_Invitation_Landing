## 1. Global UI Refinement

- [x] 1.1 Remove default tap highlight color in global styles (`styles.css` or equivalent).

## 2. Orientation Warning Implementation

- [x] 2.1 Add the orientation warning overlay structure to the main application template.
- [x] 2.2 Style the overlay to be full-screen, blocking, and centered.
- [x] 2.3 Implement CSS media queries to toggle the overlay visibility based on portrait orientation on mobile devices.

## 3. Mobile Landscape Layout Optimization

- [x] 3.1 Optimize the Hero section (font sizes, margins) for landscape mobile view.
- [x] 3.2 Adjust the photos container and marco-foto dimensions for better fit in landscape.
- [x] 3.3 Ensure the envelope opening animation and elements are correctly scaled for landscape heights.
- [x] 3.4 Move envelope and hint text 50% to the right in landscape view.
- [x] 3.5 Increase font sizes for letter content (h2 and text) in landscape view.
- [x] 3.6 Update page title to "Invitación de boda".
- [x] 3.7 Update favicon to `assets/logo.png`.

## 4. Verification

- [x] 4.1 Verify that the orientation overlay appears in portrait mode on mobile.
- [x] 4.2 Verify that the layout looks correct and fits the viewport in landscape mode.
- [x] 4.3 Confirm that no blue highlights appear when tapping elements.
- [x] 4.4 Ensure desktop view remains unaffected.
